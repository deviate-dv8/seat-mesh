kind: worker
read_first:
  - path: README.md
    note: seat-mesh worker demo index
policies:
  - id: mesh-cli
    text: seat-mesh whoami; pass --profile <dir> (see profiles/minimal)
files:
  - package.json
